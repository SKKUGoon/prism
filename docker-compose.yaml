services:
  prism-xrp:
    build:
      context: .
      dockerfile: Dockerfile
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - RUST_LOG=info
      - SYMBOLS=xrpusdt
      - TABLE_FUT=features_xrpusdt_future2
      - TABLE_SPT=features_xrpusdt_spot2
    restart: unless-stopped

  # prism-doge:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   environment:
  #     - RUST_LOG=info
  #     - SYMBOLS=dogeusdt
  #     - TABLE=features_dogeusdt
  #   restart: unless-stopped
